#include<bits/stdc++.h>
using namespace std;
int revNumber(int n)
{
    int rem;
    int rev=0;
    while(n!=0)
    {
        rem=n%10;
        rev=rev*10+rem;
        n=n/10;
    }
    return rev;
}
int main()
{
    cout<<revNumber(1233214);
}
